import SignupLoginForm from "../_components/_auth/SignupLoginForm";

export default function Auth() {
    return (
        <main className="h-full grid place-items-center">
            <SignupLoginForm />
        </main>
    );
}
